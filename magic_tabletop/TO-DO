
Make TF frame from plane equation

Publish TF frame

Get points above plane (as a PointCloud2?) using that TF frame

Push points to plane along... gaze ray? plane normal?

Change pixels of outliers to bright green (OpenCV?)